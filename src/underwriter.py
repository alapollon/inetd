import uuid 


def __init__(self, internet_object):
  self.key=internet_object
  
  
def generate(self, sequence): 
    try: 
         return uuid.uuid1(node=self.key, clock_seq=sequence)

    except: 
    

def generate_from_asset_identity(sequence_number= None) -> str:
      if sequence_number:
        asset_key=generate(sequence_number) 
      else:
          return generate_from()

